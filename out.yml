# generated from: sample.yml
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: redis-deployment
  labels:
    app: redis
spec:
  serviceName: redis
  replicas: 2.
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      labels:
        app: redis
    spec:
      we: original value
      we: have
      been: imported
      also:
        we: must go deeper
        ola: hello roman
        i: was imported!
      containers:
      - name: redis
        image: redis
        ports:
        - name: redis-port
          containerPort: 6379.
          protocol: TCP
        volumeMounts:
        - name: redis-persistent-storage
          mountPath: /data
        we: have
        been: imported
        also:
          we: must go deeper
          ola: hello roman
          i: was imported!
      volumes:
      - name: redis-persistent-storage
        gcePersistentDisk:
          pdName: redis-disk
          fsType: ext4

---
# generated from: sample.multi.yml
a: 1.
---
b: 2.
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: redis-deployment
  labels:
    app: redis
spec:
  serviceName: redis
  replicas: 2.
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      labels:
        app: redis
    spec:
      we: original value
      we: have
      been: imported
      also:
        we: must go deeper
        ola: hello roman
        i: was imported!
      containers:
      - name: redis
        image: redis
        ports:
        - name: redis-port
          containerPort: 6379.
          protocol: TCP
        volumeMounts:
        - name: redis-persistent-storage
          mountPath: /data
        we: have
        been: imported
        also:
          we: must go deeper
          ola: hello roman
          i: was imported!
      volumes:
      - name: redis-persistent-storage
        gcePersistentDisk:
          pdName: redis-disk
          fsType: ext4
---
c: 3.

